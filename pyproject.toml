[project]
name = "stream-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "getstream[webrtc]>=2.2.2",
    "pre-commit>=4.3.0",
    "ruff>=0.12.8",
]
