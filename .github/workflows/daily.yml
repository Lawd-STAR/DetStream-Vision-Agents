name: Integration (daily)
on:
  schedule:
    # Runs at 03:00 UTC daily — adjust as needed
    - cron: '0 3 * * *'
  workflow_dispatch: {}

jobs:
  integration:
    uses: ./.github/workflows/_run-tests.yml
    with:
      marker: 'integration'
      extra-args: ''
    secrets: inherit